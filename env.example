# Development Mode Configuration
NODE_ENV=development

# Development Auto-Login (bypasses Google OAuth)
# Set to "true" to skip OAuth and auto-login as DEV_USER_EMAIL
DEV_AUTO_LOGIN=true
DEV_USER_EMAIL=hscopalm@gmail.com

# Session Configuration
SESSION_SECRET=local-dev-secret-change-in-production

# MongoDB Configuration (using docker-compose service name)
MONGODB_URI=mongodb://mongodb:27017/wishlist

# Frontend URL (use localhost or your machine's IP for mobile testing)
FRONTEND_URL=http://localhost

# Cookie Configuration (match the hostname you're accessing the app from)
COOKIE_DOMAIN=localhost

# Backend Port
PORT=5000

# Backend URL for nginx proxy (docker-compose service name)
BACKEND_URL=http://backend:5000

# Google OAuth Configuration (not needed when DEV_AUTO_LOGIN=true)
# GOOGLE_CLIENT_ID=your-google-client-id-here
# GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Email Service (optional - email disabled if not set)
# GOOGLE_SA_USERNAME=your-email@gmail.com
# GOOGLE_APP_PASSWORD=your-app-password
